x <- c(1, 1.2, 3.4, 0.8)
min(x)
max(x)

xrange(z) #definisco valore iniziale x(1) e valore finale x(n)
mean.g()  #media geometrica
mean:a()  #media armonica 
median()  #mediana

sigma()   #scarto quadratico medio 
sd()      #devianza standard 

var()     #varianza
ssdev()   #devianza


basicStats(x,ci=0.95) #ci fornisco un marea di dati relativi allla statistica riassuntiva 
                      #Package: fBasics

stat.desc(x, p=0.8)   #ci fornisce una miriadi di dati rispetto statistiche desc.
                      #x vettore dimensione n
                      #p=1-alpha livello di confidenza     
                      #Package: pastecs   

tabulate()            #definisce una tabella, nella prima colonna mette i valori
                      # e nella seconda la frequenza di tale elemento


hist(x, breaks = a,   #istogrammma , devo definire un sacco di variabili
+right = FALSE,
+include.lowest = FALSE,
+plot = FALSE)$break

>x <- c(51.1, 52.3, 66.7, 77.1, 77.15, 77.17)   #dati
> n <- 6
> m <- 4
> a1 <- 50
> a2 <- 65
> a3 <- 70
> a4 <- 85
> a <- c(a1, a2, a3, a4)
> b1 <- 65 - 50
> b2 <- 70 - 65
> b3 <- 85 - 70
> b <- c(b1, b2, b3)


#VARIABILI DISCRETE & CONTINUE
PDF:StatistiC_R
PAG:236-237-238